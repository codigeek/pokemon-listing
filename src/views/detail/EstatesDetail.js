import React from 'react';
import Detail from 'components/list/Detail';

const EstatesDetail = (props) => {
  return (
    <>
      <Detail {...props} />
    </>
  );
};

export default EstatesDetail;
