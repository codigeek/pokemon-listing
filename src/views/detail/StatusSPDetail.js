import React from 'react';
import Detail from 'components/list/Detail';

const StatusSPDetail = (props) => {
  return (
    <>
      <Detail {...props} />
    </>
  );
};

export default StatusSPDetail;
